<p id="notice"><%= notice %></p>

<h1>User</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.event.each do |event| %>
      <tr>
        <td><%= event.title %></td>
        <td><%= event.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
